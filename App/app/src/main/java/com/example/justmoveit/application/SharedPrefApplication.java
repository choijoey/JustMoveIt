/*
package com.example.justmoveit.application;

import android.app.Application;
import android.content.SharedPreferences;
import android.preference.PreferenceManager;

import com.example.justmoveit.model.Ticket;

public class SharedPrefApplication extends Application {
    private static SharedPreferences movieSP, userSP;
    private static SharedPreferences.Editor movieEditor, userEditor;

    public void SaveReservedTicket(Ticket ticket){
        userSP = getSharedPreferences("userInfo", MODE_PRIVATE);
        editor = userSP.edit();
        editor.putString(key, value);
        editor.commit();
    }
    public void getReservedTickets(){
        userSP = getSharedPreferences("userInfo", MODE_PRIVATE);
        userEditor = userSP.edit();
        userEditor.putString(key, value);
        userEditor.commit();
    }
}
*/
